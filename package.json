{
  "scripts": {
    "dev:scss": "sass --watch index.scss src/style.css",
    "dev:server": "live-server",
    "run-all":"npm-run-all --parallel dev:*"
  },
  "dependencies": {
    "bootstrap": "^5.2.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.55.0"
  }
}
