{
  "scripts": {
    "start": "sass --watch index.scss style.css && live-server"
  },
  "dependencies": {
    "bootstrap": "^5.2.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.55.0"
  }
}
